<h1>Sidor för siten: <%= @mother_site.title %></h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Meta description</th>
      <th>Site</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @pages.each do |page| %>
      <tr>
        <td><%= page.title %></td>
        <td><%= page.meta_description %></td>
        <td><%= page.site_id %></td>
        <td><%= link_to 'Show', page %></td>
        <td><%= link_to 'Edit', edit_site_page_path(page) %></td>
        <td><%= link_to 'Destroy', page, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Page', new_site_page_path(@mother_site) %>
