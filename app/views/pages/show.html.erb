<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @page.title %>
</p>

<p>
  <strong>Meta description:</strong>
  <%= @page.meta_description %>
</p>

<%= @page.sections.each do |section| %>
	<%= link_to section.title, section %>
<%end%>

<%= link_to 'Redigera', edit_page_path(@page) %> |
<%= link_to 'Back', @page.site %>
